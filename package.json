{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "WRANGLER_SEND_METRICS=false wrangler pages dev examples --local",
    "start-stackblitz": "WRANGLER_SEND_METRICS=false wrangler pages dev examples --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.0.0",
    "@types/service-worker-mock": "^2.0.1",
    "service-worker-mock": "^2.0.5",
    "tsm": "^2.2.1",
    "wrangler": "2.20.1"
  }
}
